import React from 'react';

// type CardListPropsType = {
//   children: React.ReactNode;
// };

const CardList: React.FC = ({ children }) => <ul className="list">{children}</ul>;

export default CardList;
